package com.ppsdevelopment.service;

import com.ppsdevelopment.domain.dictclasses.AliasSettings;
import org.springframework.stereotype.Component;
import org.springframework.web.context.annotation.SessionScope;
import java.io.Serializable;
import java.util.HashMap;
import java.util.Map;
/*

@Component
@SessionScope
public class MainTableSessionService implements Serializable {

    private Map<Long, AliasSettings> aliasSettings=new HashMap<>();

    public Map<Long, AliasSettings> getAliasSettings() {
        return aliasSettings;
    }

    public void setAliasSettings(Map<Long, AliasSettings> aliasSettings) {
        this.aliasSettings = aliasSettings;
    }

}
*/
